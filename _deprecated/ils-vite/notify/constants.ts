export const NOTIFY = 'notify@'

// ACTIONS
export const NOTIFY_SUCCESS = `${NOTIFY}SUCCESS`
export const NOTIFY_LOADING = `${NOTIFY}LOADING`
export const NOTIFY_WARNING = `${NOTIFY}WARNING`
export const NOTIFY_INFO = `${NOTIFY}INFO`
export const NOTIFY_ERROR = `${NOTIFY}ERROR`
export const LOADING_START = `${NOTIFY}LOADING_START`
export const STOP = `${NOTIFY}STOP`

// STATUS
export const LOADING_VISIBLE = `${NOTIFY}LOADING_VISIBLE`
export const IN = `${NOTIFY}IN`
export const OUT = `${NOTIFY}OUT`
export const HIDDEN = `${NOTIFY}HIDDEN`
