import { dispatcher } from 'reduxed'

export function {{camelCase name}}Bee({ state, getState, action, extra }){
  console.log({
    name: '{{name}}'
    state,
    getState: typeof getState,
    action,
    extra
  })


}